# secrets — never commit
.env
.env.*
!.env.example

# build output
/nocturn
/nocturnd
*.test

# interpreter build cache (wasi-sdk + quickjs-ng checkout; see internal/script/qjs/build.sh)
internal/script/qjs/.buildcache/

# macOS
.DS_Store

# local runtime plugins (not source; testdata/example documents the format)
/plugins/

# per-workspace runtime state (mnt data, .skills, grants.json) — not source.
# Anchored to the repo root so it does NOT also swallow internal/workspace/
# (the composition-aggregate package — real source).
/workspaces/
