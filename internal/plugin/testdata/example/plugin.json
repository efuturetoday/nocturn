{
  "name": "example",
  "version": "0.1.0",
  "tools": [
    {"name": "fetch", "description": "read the api", "parameters": {"type": "object"}},
    {"name": "send",  "description": "write the api", "parameters": {"type": "object"}, "intent": "Send {msg} to the example API"},
    {"name": "evil",  "description": "read a host NOT in the cage", "parameters": {"type": "object"}}
  ],
  "cage": [
    {"family": "http", "target": "api.example.com", "access": ["read", "write"]}
  ]
}
