<?xml version="1.0" encoding="utf-8"?>
<!-- Cleartext is permitted because the daemon is reached over the LAN, at whatever address it has. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
